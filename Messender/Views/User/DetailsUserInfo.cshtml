@using Microsoft.AspNet.Identity
@model Messender.Models.ApplicationUser
<div class="modal-content" style="width: 69%">
    <div class="modal-header">
        <button class="close" data-dismiss="modal" area-hidden="true">X</button>
    </div>
    <div class="modal-body">
        <label>
            @if (Model.Image == null)
            {
                <img class='rounded-circle' style='width:75px; height:80px;' src="@Url.Content("~/Image/user.jpeg")" alt="">

            }
            else
            {
                @Html.Raw("<img class='rounded-circle' style='width:75px; height:80px;' src=\"data:image/jpeg;base64,"
                                            + Convert.ToBase64String(Model.Image) + "\" />")
            }
            @Html.DisplayFor(m => m.FirstName) @Html.DisplayFor(m => m.LastName)
        </label>
        <div id="ErrorMessage"></div>
        <table class="table">
            @if (Model.Email != null)
            {
                <tr>
                    <td>
                        Email:
                    </td>
                    <td>
                        @Html.DisplayFor(m => m.Email)
                    </td>
                </tr>
            }
            @if (Model.DateOfBirth != null)
            {
                <tr>
                    <td>
                        Date of bitrh:
                    </td>
                    <td>
                        @Html.DisplayFor(m => m.DateOfBirth)
                    </td>
                </tr>
            }
            @if (Model.PhoneNumber != null)
            {
                <tr>
                    <td>
                        Phone number:
                    </td>
                    <td>
                        @Html.DisplayFor(m => m.PhoneNumber)
                    </td>
                </tr>
            }
            @if (Model.Address != null)
            {
                <tr>
                    <td>
                        Address:
                    </td>
                    <td>
                        @Html.DisplayFor(m => m.Address)
                    </td>
                </tr>
            }
            @if (Model.Sity != null)
            {
                <tr>
                    <td>
                        City:
                    </td>
                    <td>
                        @Html.DisplayFor(m => m.Sity)
                    </td>
                </tr>
            }
            @if (Model.State != null)
            {
                <tr>
                    <td>
                        State:
                    </td>
                    <td>
                        @Html.DisplayFor(m => m.State)
                    </td>
                </tr>
            }
            @if (Model.County != null)
            {
                <tr>
                    <td>
                        Country:
                    </td>
                    <td>
                        @Html.DisplayFor(m => m.County)
                    </td>
                </tr>
            }
            @if (Model.Gender != null)
            {
                <tr>
                    <td>
                        Gender:
                    </td>
                    <td>
                        @Html.DisplayFor(m => m.Gender)
                    </td>
                </tr>
            }
        </table>
        @*@if (ViewBag.Id == User.Identity.GetUserId())
            {


            }
            else
            {
                @Html.ActionLink("Add to friend", "SendRequest", "Friend", new { @friendSecond = Model.Id, @friendFirst = ViewBag.Id }, null)
            }*@
        @Html.ActionLink("Add to friend", "SendRequest", "Friend", new { @friendSecond = Model.Id, @friendFirst = ViewBag.Id }, null)
    </div>
</div>

